// const logConfig = require('../modlog/modlog.json');

// function mRemove(member) {
//   console.log('test');
//   const guildId = member.guild.id;
//   return guild.channels
//     .find((channel) => channel.id === logConfig[guildid])
//     .send(`${member.id} has left the server`);
// }

// module.exports = { remove: modLoggin };
